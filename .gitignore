__pycache__/
*.py[cod]
.venv/
venv/
env/
.idea/
.vscode/
*.egg-info/
.DS_Store

# Generated working files. weights.json is the fitted model calibrate.py writes
# and run.py/backtest.py read; alert.json is the pending webhook message run.py
# stages for send_alerts.py. Both are regenerated every run and neither belongs
# in the history — one `git add .` after following the README used to commit
# the model file into the repo root.
weights.json
alert.json

# The two large reductions of the price history. They are rebuilt from scratch
# every run and deployed to Pages from the build, so committing them only grew
# the history by ~1.3 MB a day of near-identical JSON. Everything the pipeline
# needs to *remember* between runs (iv_history.csv, universe.json,
# calibration.json) and the scan itself stay committed.
public/data/charts.json
public/data/weekly.json
